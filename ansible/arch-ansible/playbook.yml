---
- name: Apply Arch config mirrored from nixos-flake
  hosts: arch_hosts
  gather_facts: true
  become: true

  roles:
    - role: system
      tags: [system]
    - role: user_config
      tags: [user, dotfiles]
